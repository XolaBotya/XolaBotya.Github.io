<?php
// Check if a payment status parameter exists and is 'success'
//if (isset($_GET['status']) && $_GET['status'] === 'success') {
    // Payment was successful
    // You can perform additional processing here if needed

    // Redirect to the home page
    header("Location: https://newfields.co.za/");
  //  exit;
//} else {
    // Payment was not successful or no payment data is present
    // Handle this case accordingly, e.g., show an error message
//}

